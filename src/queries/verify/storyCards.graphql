query AdventureState($shortId: String) {
    adventureState(shortId: $shortId) {
        storyCards {
            value
        }
        user {
            profile {
                title
                thumbImageUrl
            }
        }
    }
}