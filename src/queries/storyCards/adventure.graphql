query Adventure($shortId: String) {
    adventure(shortId: $shortId) {
        storyCards {
            keys
            title
            type
            value
            description
            useForCharacterCreation
        }
    }
}