query Scenario($shortId: String, $viewPublished: Boolean) {
    scenario(shortId: $shortId, viewPublished: $viewPublished) {
        gameCodeOnInput
        gameCodeOnModelContext
        gameCodeOnOutput
        gameCodeSharedLibrary
        options {
            shortId
            title
            deletedAt
            parentScenario {
                shortId
            }
        }
        state {
            instructions
            storyCardInstructions
            storyCardStoryInformation
            storySummary
        }
        memory
        prompt
        title
        type
        storyCardCount
        authorsNote
    }
}