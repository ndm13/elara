query Adventure($shortId: String) {
    adventure(shortId: $shortId) {
        memory
        actionCount
        scenario {
            title
            shortId
        }
        details
        title
        authorsNote
        gameState
        message
        storyCardCount
    }
}